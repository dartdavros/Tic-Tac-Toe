# Clarifications: game-reducer

## Question 1
Как определить, чей ход сейчас в PvAI-режиме — нужна ли вспомогательная функция isAiTurn?

*Context:* FR-06 требует проверять 'сейчас ход ИИ', но способ определения этого не специфицирован — это влияет на структуру кода и тестируемость.

**Options:**
- A) Создать экспортируемую чистую функцию isAiTurn(status, settings): boolean, используемую внутри редьюсера и в тестах
- B) Встроить логику определения хода ИИ прямо в тело редьюсера без выделения в отдельную функцию
- C) Определять ход ИИ через сравнение status.currentPlayer с settings.humanPlayer прямо на месте (inline)

**Answer:**
Создать экспортируемую чистую функцию isAiTurn(status, settings): boolean, используемую внутри редьюсера и в тестах

## Question 2
Что должно произойти, если getBestMove возвращает null или -1 (нет доступных ходов) в PvAI-режиме?

*Context:* FR-06 вызывает getBestMove синхронно, но не описывает поведение при невозможности найти ход — это может привести к некорректному состоянию доски.

**Options:**
- A) Считать это ничьей: перевести screen в 'result', status в { kind: 'draw' }
- B) Логировать ошибку через logEvent и вернуть текущее состояние без хода ИИ (ход игрока уже применён)
- C) Выбросить исключение внутри редьюсера — оно будет перехвачено try/catch из FR-09, состояние откатится полностью
- D) Пропустить ход ИИ молча, передать ход обратно игроку

**Answer:**
Логировать ошибку через logEvent и вернуть текущее состояние без хода ИИ (ход игрока уже применён)

## Question 3
Должен ли RESTART сбрасывать настройки режима (settings) к дефолтным или сохранять текущие настройки партии?

*Context:* FR-07 говорит 'возврат к INITIAL_STATE', но INITIAL_STATE содержит mode: 'pvp' — это может быть нежелательно, если игрок хочет переиграть в том же режиме.

**Options:**
- A) RESTART полностью возвращает INITIAL_STATE включая settings: { mode: 'pvp' } и screen: 'menu'
- B) RESTART сбрасывает только доску и статус, сохраняет текущие settings и переводит screen: 'game' (новая партия в том же режиме)
- C) RESTART сохраняет settings, переводит screen: 'menu' (игрок видит меню с предыдущими настройками)

**Answer:**
RESTART полностью возвращает INITIAL_STATE включая settings: { mode: 'pvp' } и screen: 'menu'

## Question 4
Нужно ли логировать успешные ходы через logEvent, или logEvent вызывается только при ошибках?

*Context:* FR-09 явно требует logEvent только при исключениях, но Open Questions в спецификации оставляют этот вопрос открытым — решение влияет на количество вызовов логгера.

**Options:**
- A) Логировать только ошибки: logEvent вызывается исключительно в блоке catch (как явно указано в FR-09)
- B) Логировать каждый успешный ход: logEvent('move_made', { index, player }) после применения хода
- C) Логировать ключевые события: начало игры (START_GAME) и завершение партии (переход в 'result')

**Answer:**
Логировать только ошибки: logEvent вызывается исключительно в блоке catch (как явно указано в FR-09)

## Question 5
Как должен вести себя редьюсер при START_GAME в PvAI-режиме, если ИИ ходит первым и getBestMove возвращает ход — должен ли этот ход ИИ проверяться на победу/ничью?

*Context:* FR-03 говорит 'вычисляется и применяется первый ход ИИ', но не уточняет, нужно ли после этого вызывать checkWinner — на пустой доске победа невозможна, но это создаёт архитектурный прецедент.

**Options:**
- A) Да, вызывать checkWinner после первого хода ИИ при START_GAME — для единообразия с FR-05/FR-06
- B) Нет, не вызывать checkWinner при START_GAME — на пустой доске победа невозможна, проверка избыточна
- C) Переиспользовать общую вспомогательную функцию applyMove(board, index, player): { board, gameResult }, которая всегда проверяет победителя

**Answer:**
Да, вызывать checkWinner после первого хода ИИ при START_GAME — для единообразия с FR-05/FR-06

## Question 6
Какой тип должен иметь параметр action в блоке catch FR-09 при вызове logEvent — полный объект GameAction или только его тип (action.type)?

*Context:* logEvent('reducer_error', { action, error }) из FR-09 передаёт весь action, но это может содержать чувствительные данные или вызвать проблемы сериализации при определённых payload.

**Options:**
- A) Передавать полный объект action: logEvent('reducer_error', { action, error }) — как указано в FR-09
- B) Передавать только тип действия: logEvent('reducer_error', { actionType: action.type, error }) — безопаснее и достаточно для диагностики
- C) Передавать строковое представление: logEvent('reducer_error', { action: JSON.stringify(action), error: error.message })

**Answer:**
Передавать полный объект action: logEvent('reducer_error', { action, error }) — как указано в FR-09
