/**
 * Публичный API каталога ErrorBoundary.
 * Реэкспортирует только ErrorBoundary как единственную точку входа.
 * ADR-008: co-location — все файлы компонента в одном каталоге.
 */
export { ErrorBoundary } from './ErrorBoundary';